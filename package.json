{
    "name": "6502-js",
    "private": true,
    "scripts": {
        "typecheck": "tsc --noEmit",
        "test": "bun run test-runner.ts",
        "dsl": "bun run dsl-runner.ts",
        "web:build": "tsc -p web/tsconfig.json",
        "web:serve": "bun run web/serve.ts"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "@glideapps/ts-necessities": "^2.4.0"
    }
}
