{
    "name": "6502-js",
    "private": true,
    "type": "module",
    "scripts": {
        "typecheck": "tsc --noEmit",
        "test": "node --experimental-strip-types --test tests/**/*.test.ts",
        "test:coverage": "node --experimental-strip-types --test --experimental-test-coverage tests/**/*.test.ts",
        "basic": "node --experimental-strip-types basic-runner.ts",
        "dsl": "node --experimental-strip-types dsl-runner.ts",
        "web:build": "tsc -p web/tsconfig.json",
        "web:serve": "node --experimental-strip-types web/serve.ts"
    },
    "devDependencies": {
        "@types/node": "^20.0.0"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "@glideapps/ts-necessities": "^2.4.0"
    }
}
